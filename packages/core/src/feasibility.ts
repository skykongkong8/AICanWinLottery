import type { Feasibility } from "@lotto/shared";
import { combinationKey } from "./validation.js";
export type FeasibilityResult = { feasibility: Feasibility; effectiveCount: number };
export function availableCombinationCount(luckyCount: number): number { const choose = (n:number,k:number):number => { if(k<0||k>n) return 0; let r=1; for(let i=1;i<=k;i++) r=(r*(n-k+i))/i; return Math.round(r); }; return choose(45-luckyCount, 6-luckyCount); }
export function assessFeasibility(luckyNumbers: number[], requestedCount: number, jackpotKeys: Set<string>): FeasibilityResult { if (luckyNumbers.length === 6) { const forced = [...luckyNumbers].sort((a,b)=>a-b); if (jackpotKeys.has(combinationKey(forced))) return { effectiveCount: 0, feasibility: { status: "INFEASIBLE_LUCKY_SET", requestedCount, effectiveCount: 0, partial: false, message: "The six lucky numbers exactly match a historical 1st-prize combination, so no compliant recommendation can be generated." } }; return { effectiveCount: 1, feasibility: { status: "OK", requestedCount, effectiveCount: 1, partial: requestedCount !== 1, message: "Six lucky numbers force a single combination." } }; } const available = Math.max(0, availableCombinationCount(luckyNumbers.length)); const effectiveCount = Math.min(requestedCount, available); if (effectiveCount < requestedCount) return { effectiveCount, feasibility: { status: "PARTIAL", requestedCount, effectiveCount, partial: true, message: "Requested count exceeds available unique non-jackpot combinations; returning the maximum available." } }; return { effectiveCount, feasibility: { status: "OK", requestedCount, effectiveCount, partial: false } }; }
